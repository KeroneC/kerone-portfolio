Place Kerone's real profile image here.

Recommended filename:

kerone-profile.png

Expected public path:

/images/profile/kerone-profile.png

After adding the image, set `profileAssets.profileImage.isAvailable` to `true`
in `src/data/profile.ts`.
